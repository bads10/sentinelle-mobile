# Variables d'environnement - Sentinelle Backend
# Copier en .env pour le développement local (jamais committer .env)

# Port d'écoute (Railway/Render injectent $PORT automatiquement)
PORT=8000

# Environnement (development | production)
ENV=development
